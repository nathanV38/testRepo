<h1>Liste des postes</h1>

<table>
  <tr>
    <th>Nom</th>
    <th>Titre</th>
    <th>Contenu</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.title %></td>
    <td><%= post.content %></td>
    <td><%= link_to 'Lire', post %></td>
    <td><%= link_to 'Modifier', edit_post_path(post) %></td>
    <td><%= link_to 'Effacer', post, method: :delete, data: { confirm: 'êtes vous certain?' } %></td>
   
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouveau Poste', new_post_path %>
